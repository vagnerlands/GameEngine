#version 330 core
out vec4 FragColor;

in VS_OUT {
    vec3 FragPos;
    vec2 TexCoords;
} fs_in;

uniform sampler2D image;
uniform vec4 color;

void main()
{           
	// Activate to DEBUG
    //FragColor = texture(image, fs_in.TexCoords) * color;
    FragColor = color;
}

